<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CampusConnect - <%= title %></title>
  <link rel="stylesheet" href="/styles.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <style>
    body, html { height: 100%; margin: 0; overflow: hidden; }
    .main-wrapper { display: flex; height: 100vh; }
    .sidebar { min-width: 200px; height: 100vh; position: fixed; overflow-y: auto; top: 0; left: 0; }
    .content-area { margin-left: 200px; margin-right: 250px; overflow-y: auto; height: 100vh; padding: 2rem; background-color: #f8f9fa; flex: 1; }
    .content-area::-webkit-scrollbar { display: none; }
    .trending-section { width: 250px; height: 100vh; position: fixed; right: 0; top: 0; padding: 1rem; background-color: #ffffff; overflow-y: auto; border-left: 1px solid #ccc; }
    .post-image { width: 100%; max-height: 400px; object-fit: contain; }
    .trending-section h5 { font-weight: bold; margin-bottom: 1rem; }
    .trending-section a { display: block; color: #007bff; text-decoration: none; margin-bottom: 10px; }
    .trending-section a:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <div class="main-wrapper">
    <%- include('partials/navbar') %>

    <main class="content-area">
      <%- body %>
    </main>

   
  </div>
</body>
</html>
